{% extends 'base.html.twig' %}

{% block title %}Edition d'un personnage{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edition d'un personnage</h1>

        {{ include('character/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-warning mt-3 mb-3" href="{{ path('character_index') }}">back to list</a>

        {% if is_granted("ROLE_SUPER_ADMIN") %}
            {{ include('character/_delete_form.html.twig') }}
        {% endif %}
    </div>
{% endblock %}
